# PR-00 阶段审查

基线 2cd58e9；审查包含未提交文件。两名只读审查者独立检查 Standards 与 Spec。PR-00 仍未通过。

## Standards

- P1：PostgreSQL 包装器超时仅终止 Docker 客户端，匿名测试容器可能残留。已为两容器分配独立名称及同轮 label，finally 检查并清理双方；启动超时与身份不匹配回归通过，仍需实际 PG 复验。
- P2：Python/Node 超时未清理后代进程。尚未修复，是下个安全任务。
- P2：TS 扫描器只检查文件本身链接，未检查父目录。已增加全祖先检查及目录链接/越界回归，1 项 Node 测试通过。
- 判断性建议：XLSX 内容确定但 ZIP 元数据未规范化，不能将随机文件哈希变化直接视为业务差异；镜像和 Python 范围依赖仍需解析清单证明可重建。

## Spec

- 已知缺项：静态调用表尚未形成实际跨模块执行链；部署入口的运行文件哈希/计划任务未完整核验；普通表格/Pi 成果 E2E 尚未完成，Native 因迁移缺表失败。
- 新发现：progress 缺分支、待执行命令、兼容开关及下个任务，且状态过时；本轮已补充。PR-00 范围记录漏 CI/backend 测试，本轮已纠正。
- 新发现：TS 未完整分类组件、模型调用、CSS、构建依赖和 query key 符号。待补，不用调用短名称替代全部分类。
- 新发现：重复候选未建立四个来源根的相对路径/来源关系，也未区分 vendor 与生成文件。待补，不授权删除。

Standards：3 项硬性问题、2 项建议；最严重为超时后测试容器可能残留。Spec：6 项缺项；最严重为实际调用链和执行验证覆盖不足。既有失败基线保持可见，不要求通过删除测试达到 PR-00 表面全绿。
