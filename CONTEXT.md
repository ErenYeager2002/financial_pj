# 财务 Skill 平台

财务 Skill 平台负责把受控的财务能力提供给员工和管理员，并保存身份、权限、文件、任务、结果与审计记录。Clerk 只证明登录身份，平台业务权限始终以本地用户为准。

## Language

**平台用户（Platform User）**:
已经映射到平台本地账号的使用者，拥有部门、角色、状态和 Skill 权限。
_Avoid_: Clerk 用户、账号

**Skill**:
带有版本、输入约束、风险级别和受控执行入口的财务业务能力。
_Avoid_: 工具、脚本、功能插件

**基础文件 Skill（Foundation File Skill）**:
面向 Word、PDF、PowerPoint 或 Excel 文件的通用创建、编辑、读取和检查能力，不对应一个固定的财务业务流程，可以使用通用文件处理入口。
_Avoid_: 基础工具、业务 Skill

**辅助 Skill（Supporting Skill）**:
用于运行环境诊断或任务需求澄清的支持能力，不创建普通财务任务，应使用与其目的匹配的诊断界面或对话界面。
_Avoid_: 基础文件 Skill、业务任务

**业务 Skill 执行体验（Business Skill Execution Experience）**:
平台针对一个业务 Skill 的真实流程提供的完整用户界面，覆盖任务创建、业务检查点、进度、失败恢复和结果。它可以复用无业务含义的基础组件，但业务步骤、操作和结果由该 Skill 独立编排；没有配置执行体验的业务 Skill 不能退回通用任务表单运行。
_Avoid_: 通用表单、任意前端插件

**任务（Run）**:
某个用户基于固定 Skill 版本、参数和文件快照创建的一次执行记录。
_Avoid_: 作业、进程、调用

**任务事件（Run Event）**:
任务生命周期中不可变的进度、状态或结果通知。
_Avoid_: 日志、消息

**平台文件（Platform File）**:
由平台登记、带所有者和 SHA-256 的输入文件或结果文件。
_Avoid_: 附件、临时文件

**业务材料版本（Workflow Material Set）**:
同一用户、部门和 Skill 下不可变的一组权威业务工作簿，包含各年度盈亏核算表和唯一一份到账流转表。任务创建时固定读取一个版本；全部写入与回读校验通过后发布下一版本。历史恢复也必须创建新版本。
_Avoid_: 原表、最新文件、历史输出

**任务草稿（Task Draft）**:
已经结构化但不能直接执行的 Skill、参数和文件引用集合，必须由用户确认后才能创建任务。
_Avoid_: AI 任务、待执行任务

**任务提醒（Task Reminder）**:
平台通过受控的只读业务检查发现某个负责人可能需要处理的 Skill 与业务日期。任务提醒只负责告知和预填入口；用户确认 Skill、日期和业务材料后才会创建正式任务。成功处理后不再出现在待办中，但保留检查和关联记录。
_Avoid_: 自动任务、预创建任务、正式任务

**任务中心条目（Task Center Item）**:
任务中心中用于统一查找和呈现的一条记录，可以对应任务、应收核销日期执行或应收核销批次，但不改变这些对象各自的生命周期。
_Avoid_: 统一任务、后台任务

**审批记录（Approval Record）**:
针对一个不可变执行快照作出的批准、拒绝、过期或撤销记录。
_Avoid_: 确认、审核状态

**审计事件（Audit Event）**:
平台对身份、权限、文件、任务和管理操作保存的脱敏事实记录。
_Avoid_: 运行日志、调试日志

**工作流定义（Workflow Definition）**:
与固定 Skill 版本关联、由平台批准的一组受控步骤及连接关系。它描述允许怎样执行，不代表某次任务已经运行。
_Avoid_: AI 流程、自由画布

**步骤定义（Step Definition）**:
工作流定义中的一个受控执行单元，声明步骤类型、输入输出、超时、重试、风险和 Worker 路由。
_Avoid_: 节点代码、提示词步骤

**步骤执行（Step Run）**:
某次任务中一个步骤的不可变执行事实和当前状态，包含尝试次数、时间、输入输出摘要及错误。
_Avoid_: 子任务、模型思考过程

**Skill 源码绑定（Skill Source Binding）**:
平台 Skill 与外部 Git 仓库中一个确定源码目录之间经管理员确认的持久关系。名称只用于首次发现候选；绑定后以仓库、跟踪引用和源码目录定位。
_Avoid_: 同名映射、下载地址

**Skill 源码版本（Skill Source Revision）**:
一次检查或发布所固定的仓库 commit、Skill 源码目录和目录内容哈希。跟踪分支只用于发现新版本，不能作为生产版本标识。
_Avoid_: main 版本、最新源码

**Skill 发布任务（Skill Rollout）**:
将已审核的不可变 Skill 发布包切换为生产版本的一次受控操作，包含停止接收新任务、等待现有任务结束、安装、验证、恢复和重新启用。
_Avoid_: 同步脚本、目录覆盖

**取数包（Fetched Bundle）**:
一次经过完整性校验、归属于单个平台用户的原始只读取数集合，可以覆盖一个或多个明确核销日期。多日期任务共享同一取数包，但各日期的核销处理仍保持独立并严格串行。
_Avoid_: 取数预览、Skill 快照、多日合并核销、跨日写入计划

**取数预览（Fetched Data Preview）**:
从取数包派生、供工作人员检查的持久化业务视图。原始取数包清理后，历史取数预览可以继续存在，但不能用于回放。
_Avoid_: 取数包、可回放快照

**Skill 快照（Skill Snapshot）**:
任务创建时固定的 Skill 源码、版本和执行清单副本，用于保证任务按创建时的能力执行。
_Avoid_: 取数包、取数预览、业务材料版本
