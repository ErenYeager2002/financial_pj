# P3-05 可观测性、验收和部署

状态：已完成

负责人：主任务

阻塞：P3-01、P3-02、P3-03、P3-04

## 交付

- 记录脱敏模型 Trace 摘要和步骤指标。
- 增加失败率、耗时、排队、重试、人工介入和模型用量查询。
- 完成前后端全量回归、容器重建、角色冒烟和部署记录。

## 验收证据

- 观测数据不包含财务原文、凭据、完整文件路径和密钥。
- 全量测试、类型检查、lint、构建和容器健康检查通过。
- 员工、管理员各完成一条只读冒烟路径；不自动执行真实财务写入。

## 实施记录

- 新增部门级聚合观测接口和治理页卡片，包含 Run 数量/失败率、排队与执行耗时、重试、审批/人工介入、步骤指标及模型调用次数。
- 聚合只读取结构化状态、时间和计数，不返回任务原文、参数、文件路径、凭据或技术错误；跨部门污染和零耗时步骤回归通过。
- 后端全量 pytest、本轮后端文件 Ruff、前端 TypeScript、lint、导航/数据层测试和 Next 生产构建通过；全仓 Ruff 仍报告旧版 Skill 厂商脚本和历史迁移中的既有格式问题，前端仅保留 5 个原模板 lint 警告。
- 新增 `ModelTraceRecord` 脱敏摘要，只保存调用用途、模型、状态、耗时、Token 计数和失败类别，不保存提示词、响应正文或财务原文。
- 模型 Trace 至多绑定一个 Run 或任务草稿；草稿生成前的模型失败可保留无父级脱敏记录，数据库复合外键/触发器继续阻止跨 owner、跨 department 关联和双父级关联。
- 删除未消费草稿时先解除 Trace 的草稿关联，保留调用摘要；对应删除接口与回归测试通过。
- 生产镜像已重建并部署；PostgreSQL revision 为 `c5d9f3a8201b`，API、PostgreSQL、出站服务健康，Next、6 个 Worker 和网关运行中。
- PostgreSQL 并发探针、原生 SQL 完整性检查、只读合成试运行、出站配置检查和 HTTPS 登录页检查均通过。
- 员工只读试运行成功；管理员聚合读取成功，员工调用管理员权限检查被拒绝；没有执行真实财务写入。
- PostgreSQL 内事务模型 Trace 冒烟通过：草稿生成前失败调用 1 次、12 ms、输入/输出 Token 5/2；无父级失败记录可聚合，双父级约束与 Run/草稿范围复合外键存在，事务随后回滚，没有留下合成记录。
- 最终 Standards 与 Spec 双轴复核均为 PASS。
