# 06 管理页面、初始迁移与运行验收

Status: completed

Blocked by: 05

## 交付

- 增加来源发现、绑定确认、检查更新和发布进度页面。
- 初始化 16 个同名绑定候选并处理排除项、平台独有项。
- 取消旧整仓同步脚本的生产写入职责。
- 更新契约、运行手册和部署说明。

## 验收

- 管理员可以完成首次绑定和单 Skill 更新全过程。
- 员工页面不暴露仓库技术字段。
- 数据库、Registry、API 和 Worker 对当前版本的认知一致。
- 全量测试、生产构建、迁移和部署健康检查通过。

## Comments

- 2026-08-20：管理页已支持按名称发现、逐项确认绑定、检查更新、固定 Commit 打包和发布任务进度。
- 实际 Gitee `main` Commit `7f4f2d8622a3c50a8159c1ff4fc1c3a9e818be16`：16 个同名候选、1 个仓库独有、1 个排除项、3 个平台独有项，详见 `binding-inventory.md`。
- 旧 `sync_from_gitee.ps1` 未带 `-ValidateOnly` 时拒绝执行，不再承担生产写入。
- 后端全量 332 项通过；前端 54 项通过，类型、契约和生产构建通过。
- PostgreSQL 备份完成并通过临时恢复逐表校验；生产迁移为 `b7e3f9a1c5d2`，HTTPS 200，API、Next、6 个 Worker 和网关在线。
- 正式库的绑定、可用状态和发布任务记录均为 0；本次部署没有绑定或发布任何 Skill。
- 并发运行探针因已有 1 个 `waiting_approval` 工作流按安全规则拒绝执行，未取消或修改该业务任务；其余运行验收通过。
