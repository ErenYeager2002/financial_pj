# 01 文件中心只展示最新逻辑文件

状态：已完成

## 交付

- 为文件中心提供仅展示最新逻辑文件的服务端查询。
- 当前输入材料按业务材料版本展示；输出按业务含义去重，保留最新记录。
- 不删除旧文件及其任务、审计引用。

## 验收证据

- API 测试覆盖同名重复结果、不同日期核销日清和输入材料。
- 文件中心分页总数与过滤后的记录一致。

结果：API 回归覆盖同名输出去重、不同日期输出保留、当前输入保留和旧输入隐藏；全量后端测试通过。
