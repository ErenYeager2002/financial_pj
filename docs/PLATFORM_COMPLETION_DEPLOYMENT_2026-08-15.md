# 财务 Skill 平台 P3 部署验收记录

日期：2026-08-15

范围：P3-01 至 P3-07。P3-06/P3-07 于 2026-08-20 补充实施。未在部署验收中执行真实财务写入，未记录账号、会话、密钥、任务原文或业务文件路径。

## 构建与静态检查

- 后端全量 pytest：通过；退出码 0。Windows 结束清理阶段有既有临时目录权限提示，不影响测试结果。
- 本轮后端文件 Ruff：通过。全仓扫描仍有旧版 Skill 厂商脚本和历史迁移中的既有格式问题，本轮未批量改写。
- 前端 TypeScript：通过。
- 前端导航测试：5 项通过；平台数据层与统一任务向导测试：5 项通过。
- 前端 lint：退出码 0；保留 5 个原模板的嵌套组件警告，本轮文件没有新增警告。
- 本机与 Docker 中的 Next.js 生产构建：通过；保留 Google Sans Flex fallback 和 metadataBase 提示。

## 数据库与运行时

- PostgreSQL Alembic revision：`c5d9f3a8201b`。
- PostgreSQL 原生步骤完整性检查：通过。
- 并发领取探针：6 个领取者，1 个进入 running，5 个保持 queued。
- 只读合成试运行：状态 succeeded，输出文件 1 个；3 条银行记录与 3 条台账记录中匹配 2 条。
- 角色冒烟：管理员可读取部门聚合；员工管理员权限检查返回拒绝。
- 模型 Trace 聚合冒烟：在回滚事务内写入 1 次草稿生成前失败调用，聚合结果为失败 1 次、12 ms、输入/输出 Token 5/2；无父级记录可用于保存草稿生成前的失败事实，同时绑定两个父级仍被拒绝，Run/草稿的 owner 与 department 复合外键保持有效。事务回滚后未留下合成记录。

## 部署状态

- API、PostgreSQL、出站服务：healthy。
- Next.js、网关、2 个 Python Worker、2 个 HTTP Worker、2 个 Workflow Worker：running。
- 唯一宿主机入口：`127.0.0.1:8443`。
- `https://localhost:8443/auth/sign-in`：HTTP 200。
- 出站配置检查：有效，当前精确允许目标数量为 1。

## 发布边界

- AI 只生成任务草稿并进入统一任务向导，不能直接改变 Run 或 StepRun。
- 工作流拓扑现阶段只读；未开放自由代码、Shell、数据库写入或 unrestricted HTTP 节点。
- 真实财务写入仍须走已有审批、快照、文件哈希和写后验证流程。

## 2026-08-20 · P3-06/P3-07 补充验收

- PostgreSQL 已迁移到 `71d4e9c2a5f0 (head)`；迁移前备份为 `data/backups/pre-p3-material-sets-20260820-115120.dump`。
- P3-06 已建立不可变业务工作簿版本集、原子发布、并发版本检查、历史恢复、文件删除保护和管理员显式迁移工具。迁移前已启动任务在没有当前版本时安全建立 V1；存在相同当前版本时绑定；文件不同或绑定版本已过期时在写入前拒绝。
- P3-07 已在智云取数后固定暂停，弹窗使用独立滚动数据区和固定操作区；确认、缺失 AR/SO 编号补取、前端格式校验、去重、防重复提交、补取后再次确认和正确分页均已接入。
- 后端全量 pytest 通过；最终版本链兼容改动相关回归 29 项通过，Ruff 通过。P3-07 前端与路由 7 项测试、TypeScript、Lint、本机及 Docker 生产构建通过。
- 生产 API 健康，2 个 Python Worker、2 个 HTTP Worker、2 个 Workflow Worker 均运行；Next.js 镜像 `sha256:aa8b159f100fef48c3e182ca583b02c2912f03dfffff5ad27b42bfbec45c212b` 已部署，`https://localhost:8443/auth/sign-in` 返回 HTTP 200。
- 生产静态产物已核对包含“发现缺失数据”“数据完整，继续处理”“编号格式无效”和“恢复为新版本”。由于 Codex Windows/浏览器控制运行时无法创建内核资源，真实员工账号在 1366×768、1920×1080 和 125% 缩放下的点击冒烟仍需人工复核；未将该项记为自动化通过。
- 修复补取阶段未被 Workflow Worker 调度的问题，并新增调度入口回归测试。后端全量 pytest 与本轮 Ruff 均通过；后端镜像 `sha256:afd6d514bdbd20732b2fc17c71cb3161486974082e85def814b0b4919a519fd0` 已部署到 API 和 6 个 Worker。
- 部署前保留的补取动作在服务恢复后由 `workflow-1` 自动领取，执行 1 次后成功结束；错误 AR 编号返回未找到，工作流回到 `awaiting_fetched_data_confirmation`，继续等待工作人员确认，没有重复提交或推进核销写入。
- 用户已完成 P3-07 真实页面验收：1366×768、1920×1080 和浏览器 125% 缩放下，确认与补取操作无需滚到底即可看到；错误 AR/SO 编号会提示并禁用提交，有效编号可补取，补取后再次暂停。点击“数据完整，继续处理”后任务进入下一流程，服务端刷新复核为 `awaiting_apply_confirmation`、进度 100%，无待执行动作。P3-07 至此完成。

## 2026-08-20 · P3 最终关闭验收

- P3-01 至 P3-07 的任务票均为已完成；总规格同步为全部完成并部署。
- 当前前端 41 项测试、OpenAPI 契约检查、TypeScript、Lint 和 Next.js 生产构建通过；Lint 仅保留 5 条原模板嵌套组件警告。本次 P3 手工维护的 5 个前端文件已通过定向格式检查，生成的 OpenAPI 类型文件保持生成器原格式。
- 当前后端全量 pytest 与本轮 Ruff 通过；PostgreSQL 为 `71d4e9c2a5f0 (head)`，API 健康，Next.js、网关、2 个 Python Worker、2 个 HTTP Worker 和 2 个 Workflow Worker 均运行。
- 最终 Next.js 镜像为 `sha256:b17cd917b8909409fdcd82fb3b1dc42e0c204f5da15cca3e0aa41422b639d781`，已替换生产容器；`https://localhost:8443/auth/sign-in` 返回 HTTP 200。
- 真实验收任务刷新后仍为 `awaiting_apply_confirmation`、进度 100%，没有排队或运行中动作；本轮没有点击确认写入，也没有执行财务工作簿写入。

## 2026-08-20 · 应收核销任务易用性与串行执行

- 文件中心默认只展示当前业务材料和每个同名结果的最新记录；历史文件不删除，继续保留任务、版本和审计引用。
- 日期选择支持长按后滑动多选，完成后可单击取消任意一天；未来日期继续禁用。
- `ar-hexiao-daily` 已启用平台级单任务互斥。创建、失败任务重置和失败批次重试均使用数据库锁；成功、失败或取消前不能启动另一个任务。
- 工作流和批次新增业务任务号，格式为 `<skill-id>_<M>.<D>_<序号>`。UUID 继续作为内部主键和路由参数，页面文本优先展示业务任务号。
- PostgreSQL 已迁移到 `8e31b7c4d2a9 (head)`；迁移前备份为 `data/backups/pre-workflow-usability-20260820-141445.dump`。历史工作流与批次的业务号空值和重复值均为 0。
- 后端全量 pytest 与 Ruff 通过；前端 45 项测试、TypeScript、OpenAPI 契约检查、Lint、本机及 Docker 生产构建通过。Lint 仅保留 5 条原模板警告。
- 生产后端镜像为 `sha256:b460a858d39eaf276e4b36bb13bec94546d756e925f8b19d7eefbe9c4ecda94f`，Next.js 镜像为 `sha256:15a5d1e7ecf637d8f980d85184259cc4b78a68daf41f2b6920d5bea12f2580a1`；API、出站服务和 PostgreSQL healthy，6 个 Worker、Next.js 与网关运行中，登录页 HTTP 200。
- 部署时保留了现有未结束任务，没有自动取消或修改业务状态。当前有 2 个独立任务和 1 个批次未进入终态，因此新应收核销任务会按规则返回 409，直到这些旧任务成功、失败或取消。

## 2026-08-20 · 应收核销多日批次一次取数

- 多日批次接受包含周末的连续自然日，使用一次智云登录取得范围内每日独立四件套；平台只做一次批次级检查，补取时必须指定批次内核销日期。
- 批次使用共享工作区，先执行范围迁移审计，再从早到晚分别判定、校验和写入；每日验证通过的工作簿、台账和审计文件供下一日继续使用。
- 最后一天写入成功后单独生成范围报告；报告失败只重试范围报告，不重新执行已成功的日期。
- 平台后端全量 338 项及范围报告重试定向测试、前端 58 项、应收核销源码 417 项通过，源码测试另有 5 项按环境跳过；TypeScript、Lint、Ruff、OpenAPI 和 Next.js 生产构建通过。
- 应收核销 Skill 已从 1.6.2 更新到 1.6.3。生产后端镜像为 `sha256:5cc7167c1996b7d3fddbaacb5757ded4dee15b39d1360f68fd9c7ad005e52202`，Next.js 镜像为 `sha256:7eea18debf4cecfeb15056c62a2938df8449038f42012d7d8ed94209eed69173`。
- API、出站服务和 PostgreSQL healthy，6 个 Worker、Next.js 与网关运行中；登录页 HTTP 200。生产回读确认 API 与运行时 Skill 均为 1.6.3，范围取数入口和批次审核页面已包含在生产产物中。
