# 项目上下文：finance-skills

## 仓库与当前版本

- 本地目录：`D:\BESTEASY\finance-skills`
- 远程：`https://github.com/EvanLee2004/finance-skills.git`
- 当前分支：`main`
- 当前提交：`8e9800e`（2026-07-23 拉取）

## 项目目标

面向财务部的可复现 Skill 集合。业务 Skill 将业务口径和可维护配置放在自身目录；Excel、PDF、Word、PPT 等通用文档能力作为基座使用。真实财务源数据不进入仓库。

## 本次关注的 Skill：ar-hexiao-daily

- 用途：应收核销日清；第一版以只读判定、审核清单和挂账重扫为主，不写智云或用户原始 Excel。
- 当前版本：v2.5，已包含到账流转表匹配、计划校验、源文件校验、挂账复扫，以及向副本无损落地的执行脚本。
- 核心约束：分笔回款无逐 SO 金额时不分摊；金额由脚本计算；不确定案例转挂账或人工处理；不使用行号作为业务定位。

## 当前验证状态

- 2026-07-23 在本机运行：`python -m pytest skills/ar-hexiao-daily/tests -q`
- 结果：`103 passed, 7 skipped`。
- 跳过项依赖本地未随仓库提供的真实夹具、全年盈亏表或样例日记账；不能将其视为已在本机完成真实数据回放。

## 会话约定

每次进入本仓库先读取本文件，再检查 `git status -sb` 和相关 Skill 的 `SKILL.md`；修改业务逻辑后至少运行相应测试与输入源只读校验。

每次在本项目中的对话结束前，追加一条“会话记录”：请求、执行的操作、产生或更新的文件、验证结果、Git 状态，以及未完成事项。仅记录必要的路径、计数和结论，不写入真实财务明细或敏感业务数据。

## 会话记录

### 2026-08-13｜跨月计提月份允许使用盈亏收款时间判断

- 请求：将跨月计提补填的月份判断改为可直接查看盈亏核算表“收款时间”。
- 规则：历史月份仍优先读取当前工作区以前日期的 `判定结果_YYYYMMDD.json`；按 SO+SOD 找不到历史日清时，直接使用对应盈亏行的收款时间判断月份。收款时间与本批月份不同则确认跨月，同月不进入跨月提示；历史日清和收款时间都缺失或无效时才标“待确认”。
- 修改：更新 `classify_hexiao.py`、日清列名、Skill 核心规则、业务规则、数据契约、明妹说明及专项测试；同步到 `D:\codex\skills\ar-hexiao-daily` 和 `C:\Users\lishang\.agents\skills\ar-hexiao-daily`，6 个关键文件哈希一致。
- 验证：专项 `16 passed`；完整回归 `375 passed, 5 skipped`；ruff、py_compile、`git diff --check` 和三份 Skill 结构校验通过。
- 8 月 11 日复核：使用正式工作区的写前备份重新生成判定和《核销日清》，未再次执行写表。`SO26030263` 的 4 个历史 SOD 均按盈亏收款时间 `2026-06-23` 确认跨月，历史月份为 `2026-06`，来源显示“盈亏核算表收款时间”；新日清严格轻量审计通过。
- 安全边界：未重复修改盈亏或流转工作副本，未提交或推送；仓库无关未跟踪目录保持不动。

### 2026-08-13｜按最新规则复跑核销日期 2026-08-11

- 请求：运行前天（2026-08-11，周二）的完整应收核销任务。
- 工作区：`D:\Users\lishang\Desktop\codex8.10\核销工作区_20260811_最新规则复跑_20260813_094031`。2026 盈亏与流转副本承接截至 8 月 10 日的已完成状态，2025 盈亏副本来自用户提供的原始表；三份复制结果与来源 SHA-256 一致。
- 取数：尝试重新登录智云失败，未取得新会话；随后复用 2026-08-12 已保存的 8 月 11 日四件套，取数版本明确为 `2026-08-11-project-delivery-date-v3`，四件套及摘要复制后逐文件哈希一致。该批共 12 笔到账、42 条订单行，来源覆盖 AR/SO 38/38，74/74 条原始核销记录均有处置。
- 判定与写入：可处理 38、挂账 3、异常 1；写前校验可写 38、幂等 0、冲突 0。盈亏完成 38 个核销任务，并因 SO 全部 SOD 结清额外补填 4 条历史空白计提；涉及 2025、2026 两张盈亏副本。流转计划自动 9、手填 3，前置阶段实际新增 8 笔、1 笔已一致跳过，盈亏回读后完成状态及红字回填。
- 跨月提示：`SO26030263` 下 4 个历史 SOD 进入《核销日清》的 `跨月计提补填` sheet。完整历史日清中仍无这些 SOD 的旧日判定记录，因此历史核销月份按规则标记“待确认”，盈亏收款时间仅作线索，不冒充核销日期；计提补填本身已通过 SO 全结清闸和写前校验。
- 验证：写后重新判定为可写 0、幂等跳过 38、冲突 0；212 个受控文件只读校验通过。日清、流转变更、分年度及合并变更清单、订单差异均通过严格轻量审计。长期盈亏工作副本按业务需要保留原表历史外链和数据透视部件，使用 `W_EXTERNAL_LINKS`、`W_PIVOT_PARTS` 逐项放行；便携版已移除外链，只放行原表既有数据透视部件。无强制全量重算错误。
- 安全边界：原始 2025/2026 盈亏来源和截至 8 月 10 日流转来源与写前备份哈希一致；智云未写。未提交或推送代码。

### 2026-08-13｜SO 全部结清时识别并提示跨月计提补填

- 请求：当本次只核销同一 SO 的部分 SOD、但本次完成后该 SO 全部 SOD 已结清时，除按既有规则补填历史已核销 SOD 的空白计提外，还要识别此前不同月份已核销但未计提的 SOD，并单独告知使用者。
- 操作：历史核销月份优先读取当前工作区此前的 `判定结果_YYYYMMDD.json`，按 SO+SOD 精确匹配其真实 `hexiao_date`；不从 SO/SOD 编号推断，也不把盈亏表收款时间当作核销日期。证据确认跨月时标记“是”；缺少历史日清时仍列入提醒并标记“待确认”，收款时间仅作为候选线索。新增《核销日清》的 `跨月计提补填` sheet，并在 `先看这里` 汇总 SO 与历史 SOD 数量；补填继续受原有 SO 全部 SOD 结清闸、写前校验、冲突保护及写后回读约束。
- 修改：`classify_hexiao.py`、`build_worklist.py`、规则和数据契约、明妹使用说明及专项测试；源码同步至 `D:\codex\skills\ar-hexiao-daily` 与 `C:\Users\lishang\.agents\skills\ar-hexiao-daily`。
- 验证：完整回归 `373 passed, 5 skipped`；源码和两个安装版结构校验通过；5 个关键同步文件 SHA-256 一致。模拟《核销日清》含 3 个 sheet、0 个公式，严格轻量化审计通过。
- 安全边界：未运行真实核销、未修改业务工作簿、未同步财务平台、未提交或推送；仓库原有未跟踪目录保持不动。

### 2026-08-06｜恢复非整笔回款条件重复核销约束

- 请求：恢复此前被取消的条件重复核销判断，避免同一父回款中的同形核销记录因记录NUM不同而直接双计。
- 操作：在保留整笔回款守恒、费用计入父总额、跨父累计和幂等修复的前提下，恢复非整笔回款父子金额约束。逐单合计超过父总到账 1 元以上时，才按同一父 AR、SO、金额和币种识别精确重复组；每组保留一个记录号。无法通过精确重复完整解释超额时整笔挂账。同步更新判定码、业务规则、取数判定说明、决策树、数据契约和专项测试，并同步实际安装目录。
- 验证：源码版测试 `323 passed, 5 skipped`；安装版测试 `328 passed, 5 skipped`。源码与安装版 9 个变更文件 SHA-256 全部一致。使用现有 SO26040144 审计数据只读回放，目标重复组返回 `recovered`，逻辑保留1条、忽略1条。
- 安全边界：未修改业务工作簿、智云数据或平台内置版；未提交、未推送；仓库原有未提交改动全部保留。

### 2026-07-27｜财务 Skill 平台 MVP 架构评审

- 请求：参考星辰智能体平台，评估“网页 + Skill Registry + LLM Orchestrator + Skill Executor”的部门内部财务 Skill 平台方案是否成熟可执行。
- 结论：总体方向成熟，可作为第一期基础；保留星辰平台“管理面与执行面分离”的思想，不照搬其 RAG、多智能体和复杂工作流。大模型只负责自然语言参数提取与结果解释，脚本/RPA 负责确定性执行。
- 关键修正：执行层需拆分普通 Python Worker 与 Windows RPA Worker；长任务不能仅依赖 FastAPI 进程内后台任务；每次运行必须固化 Skill 版本、参数、输入文件哈希、输出和日志；文件 ID 由后端绑定，禁止模型生成或传入任意本地路径；普通用户可运行全部工具，但 Skill 发布/修改仅限维护者，高风险写操作仍保留单次确认。
- MVP 建议：先接入一个只读 Excel 对账 Skill，采用两角色权限、单 Skill Tool Calling、独立任务队列、SSE 进度和统一结果协议；知识库、多智能体、动态工作流与主管审批后置。
- 产出：仅更新本上下文文件，未修改任何 Skill、脚本或业务数据。
- 验证：本轮为架构静态评审，没有运行或构建平台。
- Git 状态：工作区原有未提交 RPA/Skill 文件保持不动；本轮只追加本上下文记录。

### 2026-07-23｜重新拉取与基线验证

- 请求：重新拉取 `EvanLee2004/finance-skills`，确认当前项目上下文是否已记录。
- 操作：从 `origin/main` 快进更新 `9b0bad7 → 8e9800e`；新建本上下文文件。
- 产出：本文件 `PROJECT_CONTEXT.md`。
- 验证：`python -m pytest skills/ar-hexiao-daily/tests -q` 结果为 `103 passed, 7 skipped`；工作区在拉取后与 `origin/main` 同步。
- 待办：若要验证跳过的真实夹具场景，需另行提供相应夹具、全年盈亏表或样例日记账。

### 2026-07-23｜追觅应收财务跟进表吸收（只读）

- 请求：理解 `C:\Users\lishang\Downloads\追觅应收-财务跟进表.xlsx` 的全部内容与操作口径。
- 操作：使用只读方式核对 5 个工作表的字段、记录规模、跨表发票映射、公式、回款核销状态，以及销售对账表 0715 至 0722 的快照变化；未修改源工作簿。
- 结论：工作簿由全量开票主数据、客户范围、回款/核销记录、追觅开票明细和按业务线维护的销售对账快照构成；主表与追觅开票明细可按发票号完整映射。发现追觅开票明细的金额/税额列存在未限定工作表的公式引用，不能作为金额口径，应以开票主表或价税合计重算为准。
- 验证：未发现 `#REF!` 公式；已确认销售对账表的颜色与文本共同表达“待付款、流程中/已收 PO、未收 PO、已回款”等状态，且其预计付款日期包含计算公式。
- 产出：仅更新本上下文文件；未生成业务数据副本，未提交或推送 Git。
- Git 状态：`PROJECT_CONTEXT.md` 为本地未跟踪文件（承载项目会话记录）；其他仓库内容未因本次读取而变更。

### 2026-07-23｜智云网页地址检查（只读）

- 请求：检查 `https://tf.jdy.com/ierp/index.html?formId=home_page` 网页。
- 操作：读取项目上下文与 `ar-hexiao-daily/SKILL.md`；对目标 URL 及根路径、`/ierp/`、无查询参数入口进行 HTTPS/HTTP 请求检查；未提交账号、密码或业务数据。
- 结论：目标域名当前由 OpenResty 直接返回 HTTP 404，响应为 `no Route matched with those values`；HTTP 地址仅 301 跳转到同一 HTTPS 地址，未进入登录页或业务页面。
- 验证：网页检查为只读；仓库状态仍为 `main`，仅 `PROJECT_CONTEXT.md` 未跟踪。
- 未完成事项：若需继续分析，需要提供当前有效入口、登录后的页面截图，或确认该链接是否已失效。

### 2026-07-23｜Playwright 智云登录壳

- 请求：编写使用 Playwright 打开智云页面、输入用户名和密码的 RPA 脚本。
- 操作：新增 `skills/ar-hexiao-daily/scripts/jdy_login.py` 与 `requirements-rpa.txt`；账号密码运行时输入或读取环境变量，密码不接受命令行参数；仅登录，不执行业务写操作；显式指定时才保存会话 state；失败保存诊断截图。补充 Playwright 会话文件忽略规则。
- 验证：脚本通过 `py_compile`、`--help` 和 `git diff --check`；使用本机现有 Playwright 环境访问目标 URL 时正确识别服务器 404，返回退出码 2 并生成 `工作区/rpa/` 诊断截图；真实登录未验证，因为目标地址仍未进入登录页。
- 产出：`skills/ar-hexiao-daily/scripts/jdy_login.py`、`skills/ar-hexiao-daily/requirements-rpa.txt`、`.gitignore` 更新；未写入账号、密码或业务数据。
- Git 状态：`PROJECT_CONTEXT.md`、上述脚本和依赖清单为本地未跟踪文件，`.gitignore` 已修改。
- 未完成事项：需要有效智云入口并确认登录页面选择器后，才能继续验证登录和后续只读取数流程。

### 2026-07-23｜Playwright 默认入口调整

- 请求：将 RPA 脚本默认链接改为 `http://www.jdy.com/`。
- 操作：更新 `skills/ar-hexiao-daily/scripts/jdy_login.py` 的 `DEFAULT_URL`；保留 `--url` 覆盖能力、运行时凭证输入和只登录边界。
- 验证：HTTP 入口返回 301 并跳转到 `https://www.jdy.com/`，目标站点返回 200；尚未确认该首页包含财务系统登录表单。
- Git 状态：脚本和本上下文文件仍为本地未跟踪，`.gitignore` 保持已修改状态。

### 2026-07-23｜官网入口探测与加载策略调整

- 请求：使用 `http://www.jdy.com/` 作为 RPA 默认入口。
- 操作：将脚本默认 URL 更新为该地址；将页面导航等待从 `domcontentloaded` 调整为 `commit`，并给诊断截图增加 5 秒超时，避免官网资源阻塞脚本退出。
- 验证：HTTP 入口 301 跳转至 HTTPS；Playwright 成功加载官网首页截图，但未发现用户名输入框，脚本返回退出码 2 并停止，未执行登录提交。
- 产出：更新 `skills/ar-hexiao-daily/scripts/jdy_login.py`；诊断截图写入被忽略的 `skills/ar-hexiao-daily/工作区/rpa/`。
- 未完成事项：该地址是官网首页，不是财务系统登录入口；后续需要提供实际登录页 URL，或明确官网中要点击的登录入口。

### 2026-07-23｜登录表单无限轮询

- 请求：找不到用户名或密码输入框时不要退出，持续寻找并在终端输出“正在寻找账号密码输入框”。
- 操作：更新 `skills/ar-hexiao-daily/scripts/jdy_login.py`；用户名和密码框未同时出现时每 2 秒轮询一次，移除 404/无表单立即退出的逻辑，用户可用 `Ctrl+C` 中断。
- 验证：脚本通过 `py_compile`、`git diff --check`；使用模拟页面验证了先缺用户名框、下一轮出现时会持续提示并继续，不写入账号密码或业务数据。
- Git 状态：脚本和本上下文文件仍为本地未跟踪，`.gitignore` 保持已修改状态。

### 2026-07-23｜浏览器空白与账号框轮询原因诊断

- 请求：解释浏览器页面看似刷新不出、终端持续寻找账号密码输入框的原因。
- 操作：检查官网首页响应和页面链接；未修改脚本或业务文件。
- 结论：`http://www.jdy.com/` 是官网首页，页面仅提供 `/login` 登录链接，当前页面没有用户名/密码输入框；脚本没有点击该链接，所以按无限轮询逻辑持续输出是预期行为，不是反复刷新。
- 验证：`https://www.jdy.com/login` 返回 301 到 `/login/`，随后返回 200；建议后续将登录入口改为 `/login/` 或在脚本中先点击首页登录链接。
- Git 状态：无新增业务代码修改；`PROJECT_CONTEXT.md` 仍为本地未跟踪文件。

### 2026-07-23｜登录验证码误报与按钮选择器修复

- 请求：诊断终端报“页面提示登录未完成：验证码”的登录失败。
- 操作：确认登录页存在“更新说明”弹窗、真实按钮为 `#login_btn_gray`；更新脚本自动点击官网 `/login` 链接，优先使用真实登录按钮选择器；删除泛化的“验证码”失败标记，仅保留“验证码错误”；检测验证码时暂停等待用户在浏览器中完成并点击登录；更新说明弹窗由用户手动处理后回车继续。
- 验证：脚本通过 `py_compile` 和 `git diff --check`；只读探测从官网进入 `service.jdy.com/workbench/web/account.html`，成功识别用户名框、密码框和登录按钮；未填写或提交账号密码。
- Git 状态：`jdy_login.py`、`PROJECT_CONTEXT.md` 等仍为本地未跟踪，`.gitignore` 保持已修改状态。

### 2026-07-23｜现金流量调整明细批量导出 RPA

- 请求：删除全部验证码判断；按固定流程自动点击官网登录、同意提示框、输入账号密码、进入使用、打开财务报表和现金流量表；选择最大期间查询；逐行点击“本月金额”，在现金流量调整列表选择本年/全期间、查询、全选、导出并关闭；3 秒内未弹出调整列表的行跳过。
- 操作：扩展 `skills/ar-hexiao-daily/scripts/jdy_login.py` 为只查询和导出的完整 Playwright 流程；首页登录按钮改为持续寻找；提示框自动点击“同意/我同意”；删除所有验证码文本和选择器；新增 `--login-only`、`--download-dir`、`--row-open-timeout-ms`、`--max-rows`；弹窗内的期间、查询、全选和导出均限制在调整列表容器内，避免误点背景页面。
- 安全边界：只点击登录、导航、查询、全选和导出；不点击全部重算、批量调整、自动指定、重新指定等业务写操作；终端只输出行序号与计数，不输出金额、客户或摘要。
- 验证：`py_compile`、`git diff --check` 通过；脚本中检索不到“验证码/captcha”；合成页面验证自动同意、最大期间、有效金额行识别、弹窗作用域、全选及未弹出时跳过；官网登录只读探测成功找到延迟加载的登录按钮和账号、密码、登录控件；现有测试为 `103 passed, 7 skipped`（另有 1 条 Windows GBK 解码警告）。
- 未完成事项：没有使用真实账号执行端到端下载；建议先以 `--max-rows 1` 现场试跑，确认登录后菜单和现金流量表 DOM 与截图一致，再取消限制全量导出。
- Git 状态：`jdy_login.py`、`requirements-rpa.txt`、`PROJECT_CONTEXT.md` 仍为本地未跟踪，`.gitignore` 已修改。

### 2026-07-23｜RPA 默认改用 Edge 与独立持久化配置

- 请求：解决 Playwright 浏览器加载缓慢问题，改用本机 Microsoft Edge。
- 操作：将 `skills/ar-hexiao-daily/scripts/jdy_login.py` 默认浏览器通道改为 `msedge`；新增 `--browser-channel` 和 `--user-data-dir`；默认使用被 Git 忽略的 `工作区/rpa/edge-profile` 独立配置目录保存缓存和登录会话，不读取或占用日常 Edge 用户配置；已登录工作台或出现“进入使用”时直接复用会话。
- 验证：`py_compile` 和 `--help` 通过；Playwright 实际启动 Edge 并从 User-Agent 确认 `Edg/` 通道；现有测试结果 `103 passed, 7 skipped`，保留 1 条 Windows GBK 解码警告；`git diff --check` 仅提示 `.gitignore` 的 LF/CRLF 转换。
- 安全边界：本轮未使用账号密码，未访问、输出或导出业务数据；浏览器配置目录位于 Skill 工作区且不进入 Git。
- Git 状态：`jdy_login.py`、`requirements-rpa.txt`、`PROJECT_CONTEXT.md` 仍为本地未跟踪，`.gitignore` 已修改；未提交或推送。
- 未完成事项：尚未使用 Edge 持久化配置执行真实登录与单行导出回归；首次运行仍需加载页面，后续运行才会复用缓存和会话。

### 2026-07-23｜登录改为默认人工提交

- 请求：明确该文件是普通 RPA 脚本；官网登录页加载账号框、密码框、协议框和登录按钮需要等待，不要过早自动点击，允许人工操作。
- 操作：保留账号密码输入框无限轮询；输入框出现后脚本只填写账号密码，默认不勾选协议、不点击登录、不处理后续同意弹窗；终端每 5 秒提示“正在等待人工完成登录”，检测到工作台后再继续报表流程。新增 `--auto-submit-login`，仅显式指定时使用原自动提交逻辑；人工模式禁止搭配无头运行。
- 验证：`py_compile`、`--help`、`git diff --check` 通过；调用关系检查确认协议和登录自动提交只位于 `auto_submit_login` 分支；现有测试结果 `103 passed, 7 skipped`，保留 1 条 Windows GBK 解码警告。
- 产出：更新 `skills/ar-hexiao-daily/scripts/jdy_login.py` 与本上下文文件；未使用账号密码，未访问或导出业务数据。
- Git 状态：`jdy_login.py`、`requirements-rpa.txt`、`PROJECT_CONTEXT.md` 仍为本地未跟踪，`.gitignore` 已修改；未提交或推送。

### 2026-07-23｜持续等待进入使用与现有 Edge 连接

- 请求：登录成功后“进入使用”加载较慢，不应按默认超时失败；解释并支持在用户正在使用的 Edge 中执行 RPA。
- 诊断：失败截图显示已进入企业服务页，但中央产品区域仍处于加载状态，“进入使用”按钮尚未渲染。
- 操作：将“进入使用”改为无总时限轮询，每轮输出“正在等待‘进入使用’按钮加载”；新增 `--cdp-url`，可连接启动时已开启远程调试的 Edge，并在其现有浏览器上下文中新建标签页执行；连接模式结束时不关闭用户浏览器。普通方式启动且未开放调试端口的 Edge 仍无法被 Playwright 事后接管。
- 验证：`py_compile`、`--help`、`git diff --check` 通过；延迟 1.8 秒才显示按钮、单轮等待仅 0.5 秒的合成页面验证最终成功点击；现有测试结果 `103 passed, 7 skipped`，保留 1 条 Windows GBK 解码警告。
- 产出：更新 `skills/ar-hexiao-daily/scripts/jdy_login.py` 与本上下文文件；未使用账号密码，未访问或导出业务数据。
- Git 状态：`jdy_login.py`、`requirements-rpa.txt`、`PROJECT_CONTEXT.md` 仍为本地未跟踪，`.gitignore` 已修改；未提交或推送。

### 2026-07-23｜按阿里云文章迁移为 Selenium 接管 Edge

- 请求：参考阿里云文章《使用 Selenium 调试 Edge 浏览器的常见问题与解决方案》配置 RPA，允许不再使用 Playwright。
- 操作：新增 `scripts/start_jdy_edge.ps1`，以 `--remote-debugging-port=9222` 和独立共享配置目录启动用户可见 Edge；新增纯 Selenium 实现 `scripts/jdy_selenium_rpa.py`；日常入口 `scripts/jdy_login.py` 改为 Selenium 包装器；旧 Playwright 版本改名为 `jdy_playwright_legacy.py` 仅作备份；依赖清单由 Playwright 改为 Selenium。Selenium 流程保留人工登录、无限等待“进入使用”、报表导航、最大期间查询、逐行打开调整列表、全选和导出。
- 依赖：官方 PyPI 两次下载超时后，使用清华镜像成功安装 Selenium 4.46.0 到 `D:\BESTEASY\oracleeasy--bi\.venv`；由 Selenium Manager 匹配 EdgeDriver。
- 验证：Edge 150.0.4078.83 成功监听 `127.0.0.1:9222`；Selenium 实际接管成功并读取当前页面，停止驱动服务后 Edge 保持打开；启动器重复执行可识别已开启端口；`py_compile`、`--help`、`git diff --check` 通过；现有测试 `103 passed, 7 skipped`，保留 1 条 Windows GBK 解码警告。
- 安全边界：未配置文章示例中的代理、伪造 User-Agent 或注入 Cookie；不关闭用户 Edge；本次连接验证未输入账号密码、未读取或导出业务数据。
- Git 状态：新增 Selenium RPA、Edge 启动器和旧版备份；`PROJECT_CONTEXT.md`、RPA 文件及依赖清单仍为本地未跟踪，`.gitignore` 已修改；未提交或推送。
- 未完成事项：纯 Selenium 已通过连接与静态验证，但尚未现场执行单行现金流量调整导出，建议先使用 `--max-rows 1` 回归。

### 2026-07-23｜收紧登录判定并改点本期金额

- 请求：`https://service.jdy.com/workbench/web/account.html` 在登录前后相同，不能凭 URL 判定成功；必须看到“进入使用”按钮才算登录完成。现金流量表应处理“本期金额”，不是“本月金额”。
- 操作：更新默认 Selenium 实现 `scripts/jdy_selenium_rpa.py`；删除基于 workbench URL 和“财务报表”文本的登录成功判断，唯一条件改为可见“进入使用”；进入产品流程也不再因发现“财务报表”而跳过按钮。现金流量表的框架识别、表头列定位、有效行筛选、单元格点击和终端计数全部改为“本期金额”。
- 验证：使用 Selenium 在已连接 Edge 的本地合成页面验证：无“进入使用”时返回未登录，加入按钮后才返回已登录；同时包含“本月金额”和“本期金额”的表格准确选择“本期金额”列并识别有效行。`py_compile`、`git diff --check` 通过；现有测试 `103 passed, 7 skipped`，保留 1 条 Windows GBK 解码警告；测试后 Edge 150 的 9222 端口仍正常。
- 产出：更新 `scripts/jdy_selenium_rpa.py` 与本上下文文件；未使用账号密码，未读取或导出业务数据。
- Git 状态：Selenium RPA、入口、启动器、旧版备份、依赖清单和上下文仍为本地未跟踪，`.gitignore` 已修改；未提交或推送。

### 2026-07-23｜测试凭据本地配置

- 请求：测试账号密码允许写入配置文件。
- 操作：新增被 Git 忽略的 `工作区/rpa/jdy_credentials.local.json`；Selenium RPA 新增 `--credentials-file` 并默认读取该文件。用户名优先级为命令行、环境变量、本地配置、交互输入；密码优先级为环境变量、本地配置、隐藏输入。
- 安全边界：本上下文和终端不记录具体账号密码；配置仅保存在本机 Skill 工作区，不进入 Git。
- 验证：配置 JSON 成功读取且包含两个非空字段；`git check-ignore` 确认文件由 `skills/*/工作区/**` 规则忽略；`py_compile`、`git diff --check` 通过；现有测试 `103 passed, 7 skipped`，保留 1 条 Windows GBK 解码警告。
- 产出：更新 `scripts/jdy_selenium_rpa.py`、本地凭据配置和本上下文文件；未执行登录或业务数据导出。
- Git 状态：本地凭据未出现在 `git status`；其余 RPA 文件和上下文仍为本地未跟踪，`.gitignore` 已修改；未提交或推送。

### 2026-07-23｜调整列表日期、全选与导出修复

- 请求：首个“本期金额”行打开现金流量调整列表后，脚本没有正确筛选日期、全选和导出；全选框为多个 `input.kd-checkbox-input` 中的第一个，导出按钮使用 `_2zTfwwd5 _3PwtlkVv` 类。
- 诊断：前景弹窗由可见 `#dialogShow` 承载，原弹窗根节点识别失败后退化为整页搜索，导致期间和查询误命中背景现金流量表。当前调整列表期间框实际为全年范围；导出按钮全选前带 `_2zTfwwd5 _3PwtlkVv`，全选后启用状态会只保留 `_3PwtlkVv`。
- 操作：所有调整列表操作锁定到包含标题的最上层可见 `#dialogShow`；期间必须识别为全年或显式选择“本年”，否则报错；查询必须命中弹窗并等待 `kd-checkbox-input` 生成；全选明确点击第一个 `input.kd-checkbox-input` 并验证选中状态；导出同时兼容启用前后类名，并以精确“导出”文本排除同类按钮。
- 验证：连接当前 Edge 现场验证期间为本年、弹窗查询成功、首个多选框全选成功；实际点击导出并生成 1 个测试文件，目录为 `工作区/04_产出/现金流量调整导出/selector_test_20260723_221334`。`py_compile`、`git diff --check` 通过；现有测试 `103 passed, 7 skipped`，保留 1 条 Windows GBK 解码警告。
- 产出：更新 `scripts/jdy_selenium_rpa.py`、生成 1 个被 Git 忽略的测试导出文件并更新本上下文；未执行重算、调整、重新指定等业务写操作。
- Git 状态：测试导出文件未出现在 `git status`；RPA 文件和上下文仍为本地未跟踪，`.gitignore` 已修改；未提交或推送。

### 2026-07-23｜首行弹窗立即退出修复

- 请求：诊断 `--max-rows 1` 在打开首个现金流量调整列表后立即报 `RpaError` 并退出的问题。
- 根因：逐行异常处理只输出异常类型并在截图前关闭弹窗，掩盖了具体原因；新弹窗的期间初值是单期且弹窗标题早于期间控件渲染，原逻辑只识别起止范围并立即查找控件；期间面板已展开时重复点击还会将其关闭。
- 操作：期间输入框兼容单期和范围值并等待最多 10 秒加载；优先复用已展开的期间面板，否则点击只读输入框外层控件，选择“本年”后验证范围确实变为第 1 期至第 12 期；单行失败时在关闭弹窗前保存现场截图，并输出具体 `RpaError` 原因。
- 验证：现场将第 7 期成功切换为本年，完成查询和首个 `kd-checkbox-input` 全选；随后按用户原命令完整试跑，结果为导出 1 行、跳过 0 行、失败 0 行，文件写入被 Git 忽略的时间戳导出目录。`py_compile` 通过；项目测试为 `103 passed, 7 skipped`，保留既有 Windows GBK 解码警告；`git diff --check` 仅提示 `.gitignore` 的 LF/CRLF 转换。
- Git 状态：RPA 文件和本上下文仍为本地未跟踪，`.gitignore` 已修改；未提交或推送。

### 2026-07-23｜异步全选与 Cookie 工作台复用

- 请求：修复批量处理中某一行点击第一个 `kd-checkbox-input` 后全选状态未生效的问题；浏览器已有 Cookie 时优先点击“我的工作台”，跳过账号密码登录。
- 根因：金蝶表格使用自定义复选框组件，直接点击隐藏 `input` 后页面会异步重绘；原逻辑持有旧元素并仅用 `is_selected()` 立即判断，个别行加载较慢时会误判失败。首页“我的工作台”按钮实际由 `.workstation.btn-green` 标识，精确文本定位不稳定。
- 操作：全选改为优先点击复选框的外层 `label`/可视容器，每次重新获取 DOM，最多等待 3 秒，并同时依据原生 `checked` 与“已选中 N 条”状态确认；首次点击未生效时才重新获取首个复选框做一次备用点击。登录入口新增 `.workstation.btn-green` 定位，优先点击“我的工作台”并等待“进入使用”，只有不存在有效 Cookie 时才进入账号密码流程。
- 验证：Cookie 复用现场输出“检测到‘我的工作台’，正在复用现有登录状态”，未进入账号密码填写；前 10 个可处理行回归结果为导出 7 行、跳过 3 行、失败 0 行，原第 10 行已全选并成功导出。`py_compile` 通过；项目测试为 `103 passed, 7 skipped`，保留既有 Windows GBK 解码警告；`git diff --check` 仅提示 `.gitignore` 的 LF/CRLF 转换。
- Git 状态：RPA 文件和本上下文仍为本地未跟踪，`.gitignore` 已修改；测试导出文件位于被忽略的工作区；未提交或推送。

### 2026-07-24｜全量 24 个本期金额单元格与全新页面运行

- 请求：解释本期金额有 24 个数据单元格但只生成少量导出的问题；避免复用可能停留在错误位置的旧页面；默认自动填写账号密码、勾选协议并登录。
- 根因：现金流量表使用 `kd-table-body` 虚拟滚动容器，首屏 DOM 只能观察到部分行，原脚本没有滚动收集全部 `data-row-key`；部分金额行首次点击事件未触发；Edge 启动器和脚本各开一个官网页造成重复标签；官网登录页资源持续加载会阻塞 Selenium 的普通页面加载等待。
- 操作：逐 100 像素滚动虚拟表并等待渲染，汇总、去重和排序全部本期金额 `data-row-key`，同时记录每行可靠的滚动坐标；金额点击改为优先点击内部 `cursor:pointer` 节点并默认重试 2 次。每次默认新建全新运行标签并清理专用 Edge 中旧的金蝶页面；Cookie 无效时自动转账号登录页；新增精确账号、密码、协议及登录按钮选择器，默认自动提交，保留 `--manual-login`；页面加载策略改为 `eager` 并设置 20 秒导航超时。
- 验证：现场滚动采集得到 24 个唯一的本期金额行键；完整运行结果为导出 12 行、跳过 12 行、失败 0 行，跳过项重试后仍未打开调整列表，符合“无调整明细则跳过”规则。导出目录实际存在 12 个完整 `.xls` 文件、0 个临时文件。`py_compile` 通过；项目测试为 `103 passed, 7 skipped`，保留既有 Windows GBK 解码警告；`git diff --check` 仅提示 `.gitignore` 的 LF/CRLF 转换。
- Git 状态：RPA 文件和本上下文仍为本地未跟踪，`.gitignore` 已修改；测试导出位于被忽略的工作区；未提交或推送。

### 2026-07-24｜现金流量调整导出 Skill 方案

- 请求：将已验证的金蝶现金流量调整导出 RPA 封装为可复用 Skill，先提供方案，不立即迁移。
- 方案：建议新增独立 Skill `jdy-cashflow-export`，以当前 Selenium 实现作为唯一执行脚本，配套精简 `SKILL.md`、`agents/openai.yaml`、选择器/故障参考和合成测试；凭据、Cookie、截图及导出继续只保存在被 Git 忽略的本地工作区。原 `ar-hexiao-daily/scripts/jdy_login.py` 保留兼容入口并转发到新 Skill，避免破坏现有命令。
- 边界：仅允许登录、导航、查询、全选和导出；禁止全部重算、批量调整、自动指定、重新指定等业务写操作。Playwright 暂不作为首版执行引擎，先优化已完成真实回归的 Selenium 流程。
- 验收建议：新页面启动与自动登录两条分支均通过；虚拟滚动准确发现 24 个本期金额单元格；当前测试环境完成 12 个导出、12 个无明细跳过、0 失败；导出文件数与汇总一致；凭据不进入 Git；`quick_validate.py`、脚本测试和现有项目测试全部通过。
- Git 状态：本轮仅更新项目上下文，尚未创建或迁移 Skill 文件；未提交或推送。

### 2026-07-24｜创建 jdy-cashflow-export Skill

- 请求：按已确认方案将金蝶现金流量调整导出 RPA 做成独立 Skill。
- 操作：使用 `skill-creator` 初始化 `skills/jdy-cashflow-export`；新增 `SKILL.md`、`agents/openai.yaml`、依赖清单、正式运行入口、Selenium 实现、流程/选择器参考和 2 组单元测试。原 `ar-hexiao-daily` 下的 Python 与 PowerShell 日常入口改为兼容转发，旧命令保持可用；Playwright 旧版未迁入新 Skill。
- 安全边界：Skill 明确只允许登录、导航、查询、全选和导出；禁止重算、批量调整、自动指定、重新指定等写操作。测试凭据仅复制到新 Skill 被 Git 忽略的本地工作区，源码扫描未发现凭据文本。
- 现场修复：首次全量验证发现现金流量表平衡检查提示在进入报表和点击查询后各弹出一次，第二次提示层遮挡金额点击，导致 24 行全部被误记为跳过。新增两阶段精确点击“我知道了”关闭只读提示，不点击提示中的重算入口。
- 验证：新 Skill 的 6 项单元测试、Python 编译、`quick_validate.py`、新旧入口 `--help` 均通过；既有 `ar-hexiao-daily` 测试为 `103 passed, 7 skipped`，其他有效测试模块 16 项通过，保留既有 Windows GBK 警告。修复后现场处理前 2 行，结果为导出 2、跳过 0、失败 0；导出目录存在 2 个完整文件、0 个临时文件。完整扫描仍准确识别 24 个本期金额单元格。
- 产出：新增 `skills/jdy-cashflow-export`；正式入口为 `scripts/run.py`，Edge 启动入口为 `scripts/start_edge.ps1`；运行截图、日志、凭据和导出均位于被忽略的 `工作区`。
- Git 状态：新 Skill、兼容入口、RPA 依赖清单和本上下文仍为本地未跟踪，`.gitignore` 已修改；未提交或推送。

### 2026-07-24｜合并 12 份现金流量调整导出表

- 请求：将最近完整导出的 12 张现金流量调整表整理到一张表中。
- 操作：定位批次 `20260724_093351`；将 12 个只读旧版 `.xls` 临时转换为 `.xlsx` 后检查表头和结构，按相同字段纵向合并。删除各源文件重复的 13 行合计，仅保留 5,742 条明细；新增来源文件批次列、顶部来源数/明细数/期间/金额合计公式、筛选表、冻结窗格和统一日期金额格式。
- 验证：源文件数为 12，全部表头一致；首尾数据区检查通过；公式错误扫描为 0；完成合并表首屏视觉检查并修复来源批次科学计数法显示；最终工作簿为 1 个工作表、5,742 条明细。
- 安全边界：原始 12 张表未修改；临时转换、预览和检查文件已删除，只保留被 `*.xlsx` 规则忽略的最终工作簿。
- 产出：`outputs/019f88f2-155b-7413-a407-21a8067c9383/现金流量调整明细_12表合并.xlsx`。
- Git 状态：最终财务工作簿未出现在 `git status`；其余 Skill 和上下文改动仍未提交或推送。

### 2026-07-24｜按基准现金流量明细逐凭证核对并标色

- 请求：以下载目录中的现金流量明细表为标准，按现金流量项目和凭证号核对合并表；忽略凭证号数字前导零差异，比较每组笔数与金额合计，并在合并表精确标出不一致凭证。
- 操作：只读转换旧版基准 `.XLS`；将凭证号统一为去除数字前导零的格式，并为两表 11 个同义现金流量项目建立明确别名映射。按“规范项目 + 规范凭证号”分组比较笔数和金额；在合并明细追加规范凭证号、核对结果、基准/整合笔数、基准/整合金额和差额列，并新增“核对汇总”工作表。
- 标色：金额不符或金额及笔数不符为红色，仅笔数不符为橙色，基准明细没有对应凭证为黄色，整合表缺失为紫色并列在核对汇总中。
- 结果：基准表 823 个凭证组，整合表 834 个凭证组；141 组核对一致，772 组存在金额、笔数、多出或缺失差异。用户给出的前导零凭证示例归一化后笔数与金额一致。
- 验证：两张工作表均完成关键范围检查和视觉检查；整合明细中的首个异常已确认整行标色并显示差额；公式错误扫描为 0；最终文件经本机 Excel 只读打开验证，合并明细为 5,746 行、18 列，核对汇总为 777 行、10 列。
- 产出：`outputs/019f88f2-155b-7413-a407-21a8067c9383/现金流量调整明细_凭证核对标色.xlsx`；原始基准表和原合并表均未修改。
- Git 状态：最终财务工作簿受 `*.xlsx` 规则忽略；未提交或推送。

### 2026-07-24｜创建 jdy-cashflow-reconcile Skill

- 请求：将现金流量项目与凭证号核对、笔数金额比较和异常标色流程封装为可复用 Skill。
- 操作：使用 `skill-creator` 初始化 `skills/jdy-cashflow-reconcile`；新增执行说明、界面元数据、核对规则、PowerShell 入口、确定性 JavaScript 核心、工作簿生成脚本和单元测试。入口支持旧版 `.xls` 基准表只读转换，按“规范项目 + 规范凭证号”分组，并生成逐行标色明细和异常汇总。
- 规则：凭证号统一连字符并去除数字前导零；现金流量项目只应用明确别名；空凭证号记录仍保留为待核对组；金额误差小于半分视为一致；项目集合差异通过 JSON 报告，不做模糊匹配。
- 验证：7 项归一化与分类测试通过；`quick_validate.py` 通过；使用现有基准表和 12 表合并文件完成端到端回归，计数与上一版核对结果一致；公式错误扫描为 0；两张工作表均完成预览检查，并经本机 Excel 只读打开验证。运行结束后临时转换目录、依赖 junction 和检查 sidecar 均已清理。
- 产出：`skills/jdy-cashflow-reconcile/`；端到端测试输出与预览在验证后清理，已有用户结果文件保持不变。
- Git 状态：新 Skill 与本上下文文件仍为本地未跟踪，仓库中此前的 RPA 改动保持不变；未提交或推送。
- 未完成事项：无；后续遇到新的项目名称变体时，应先由用户确认再扩展明确别名映射。

### 2026-07-24｜同号凭证跨期间聚合问题诊断

- 请求：解释核对结果中为何出现大量相同的“记-3”记录。
- 检查：只读检查基准明细表和核对结果；规范凭证号“记-3”在两表均有 25 条明细，分布于 4 个会计期间。单个期间内同一凭证还可对应多条会计分录和现金流量调整明细。
- 根因：当前 `jdy-cashflow-reconcile` 使用“规范项目 + 规范凭证号”分组，没有加入会计期间；凭证字号按月重新编号，导致不同月份的同号凭证被放入同一核对组。
- 正确口径：分组键应改为“会计期间 + 规范项目 + 规范凭证号”。同一期间同一凭证的多条明细仍应保留并汇总，不能按凭证号去重。
- 变更：本轮仅诊断并更新项目上下文，未修改 Skill 或工作簿。
- 未完成事项：需要按新分组键修改 Skill、测试并重新生成核对工作簿。

### 2026-07-27｜安装 ar-hexiao-daily Skill

- 请求：从 GitHub 仓库 `EvanLee2004/finance-skills` 安装 `skills/ar-hexiao-daily`。
- 操作：使用 Skill Installer 按 GitHub URL 下载并安装到 `D:\codex\skills\ar-hexiao-daily`。
- 验证：安装目录存在，`SKILL.md`、`scripts`、`references`、`config` 和 `tests` 均存在；该 Skill 没有 `assets` 或 `templates` 目录，符合仓库当前结构。
- Git 状态：本地仓库仅追加本会话记录；安装目标在 `D:\codex\skills`，未提交或推送。
- 未完成事项：无。

### 2026-07-27｜同步并盘点 finance-skills 全仓

- 请求：拉取 `EvanLee2004/finance-skills` 主分支，并逐个了解仓库内 Skill 的用途。
- 操作：先保护本地 `.gitignore` 改动，再 `git fetch` 与 `git pull --ff-only` 快进到 `3a642d4`；阅读根 README、15 个官方 Skill 的 `SKILL.md/README.md`、脚本入口和本地额外的两个金蝶现金流量 Skill。
- 验证：设置 `PYTHONUTF8=1` 后仓根测试为 `253 passed, 9 skipped`；本地 `jdy-cashflow-reconcile` 的 7 项 Node 测试通过；`jdy-cashflow-export` 的 2 个 Selenium 测试因依赖未安装而跳过。首次未设置 UTF-8 的测试失败属于 Windows 子进程编码兼容，不是业务断言失败。
- 结论：官方仓库为 9 个业务 Skill、2 个行为/环境 Skill、4 个文档基座；本地未提交的 `jdy-cashflow-export` 与 `jdy-cashflow-reconcile` 不属于远程主分支。后者仍有已知的跨会计期间同号凭证分组问题，使用前需将期间加入核对键。
- Git 状态：远程已同步；本地 `.gitignore`、项目上下文、RPA 文件和两个现金流量 Skill 的未提交改动均保留，未提交或推送。
- 未完成事项：无；如要把本地两个现金流量 Skill 纳入远程仓库，需要另行确认提交范围。

### 2026-07-31｜推送 ar-hexiao-daily 最新版

- 请求：将本机和财务平台已生效的 `ar-hexiao-daily 1.3.0` 更新推送到 GitHub。
- 操作：从已安装版同步业务代码、规则、文档和测试到源仓库，创建分支
  `agent/ar-hexiao-daily-1-3-0`；明确排除原有 `.gitignore`、RPA 文件、两个
  现金流量 Skill 和本上下文文件。
- 验证：源仓库 Skill 测试为 `256 passed, 5 skipped`；`quick_validate.py`
  返回 `Skill is valid!`；暂存差异的常见密钥模式扫描为 0 命中。
- 提交：`94b889f`（`feat: update ar-hexiao-daily reconciliation rules`），
  共 36 个文件、4,706 行新增、372 行删除。
- GitHub：当前账号 `abbbzaq` 对原仓库只有只读权限，直接推送返回 403；已创建
  `abbbzaq/finance-skills` fork，将分支推送到 fork，并向
  `EvanLee2004/finance-skills:main` 创建草稿 PR
  `https://github.com/EvanLee2004/finance-skills/pull/1`。
- Git 状态：本地分支已跟踪 fork 同名分支；原有未提交文件保持不变且未进入提交。
- 未完成事项：等待原仓库维护者审核并合并 PR；当前 PR 尚无自动检查结果。

### 2026-07-31｜新增写后订单差异表并更新 GitHub PR

- 请求：在确认写入完成后，对比本批实际写入订单与上传盈亏表中的写后结果，并生成差异表。
- 口径：只核对本批计划写入的订单及部分回款拆分新增行，不把上传表中的历史订单当成额外差异。
- 实现：`apply_to_copy.py` 写后回读工作副本，生成
  `04_产出/订单写入差异_<核销日>.xlsx`；工作簿包含“汇总”“订单对比”“字段差异”
  三个工作表。写入一致时字段差异为 0；不一致时列出计划值和实际值。
- 安全：人在环确认闸保持不变；本轮只修改代码、文档和测试，没有执行真实核销、
  智云写入或财务工作簿写入。
- 验证：源 Skill 与安装版完整测试均为 `260 passed, 5 skipped`；
  关键 Ruff 规则和 `quick_validate.py` 通过；样例工作簿已由 LibreOffice 实际打开并导出。
- 同步：源仓库、`D:\codex\skills\ar-hexiao-daily` 安装版和
  `financial_pj` 平台内置版的相关生产文件 SHA-256 一致。
- GitHub：新增提交 `8d40a8c`（`feat: add post-write order difference report`）
  已推送到 fork 分支，并更新现有草稿 PR
  `https://github.com/EvanLee2004/finance-skills/pull/1`；原有无关改动未纳入提交。

### 2026-08-04｜同步本地核销更新到 Gitee

- 请求：将本地已生效的应收核销日清更新同步到 Gitee，且推送过程不得弹出登录框。
- 范围：仅纳入 `skills/ar-hexiao-daily` 的 Skill 说明、直接写入流程实现和对应测试；未纳入
  金蝶 RPA 文件、浏览器工作区、导出结果、凭据或本上下文之外的未确认改动。
- 以 Gitee `main` (`22cf7f3`) 为基线建立独立工作树，提交 `4bf2dc6`：
  `feat(ar-hexiao): sync local direct-write workflow`。源 Skill 测试 `276 passed, 5 skipped`。
- Gitee：已通过 SSH Deploy Key 非交互推送到 `git@gitee.com:Lee157/finance-skills.git` 的 `main`；
  已配置仓库级 `core.sshCommand` 使用 `id_ed25519_gitee` 和 `BatchMode=yes`，后续推送不会弹出登录框，
  凭据不可用时会直接失败而不会等待输入。

### 2026-08-07｜同 SO 多 SOD 同行合并核销

- 请求：同一 SO 的多个 SOD 命中盈亏同一行时一起核销，应收金额以 SO 交付金额为准。
- 实现：只对同一父回款、同一核销记录NUM、完整 SOD 集合且两级金额均与 SO 交付金额守恒的场景放行；目标行写全部 SOD 编号，应收、计提和回款明细使用 SO 交付金额，其他成员仅保留审计且不重复写。其余同行冲突继续 E8。
- 验证：源码与两处本地安装版完整回归均为 `336 passed, 5 skipped`，`quick_validate.py` 全部通过；未修改业务工作簿或财务平台。
- Git 状态：只提交 `skills/ar-hexiao-daily` 下7个规则、脚本和测试文件；提交 `f046e2f` 已推送到 Gitee 与 GitHub 分支 `agent/ar-hexiao-daily-1-3-0`，GitHub 草稿 PR #1 已更新。仓库原有 `.gitignore`、项目记录和其它 Skill 改动未纳入提交。

### 2026-08-12｜到账流转表前置登记与写后状态回填

- 智云分类后，强三键唯一命中的到账行先写 `SO  交付金额`，每个 SO 独占一行；交付金额取分类结果中的 SO 最新本币交付金额。
- 盈亏表写入和回读全部完成后才回填“是否更新应收款”：全部完成填“是”，部分完成填“部分”，全部未完成留空；“部分”沿用原规则，只把未核销 SO 标红。
- 流转写入新增 `prefill` 与 `status` 两阶段，保留非本批 SO 的已有颜色；源码完整回归 `364 passed, 5 skipped`，两个本机安装目录的生产文件 SHA-256 与源码一致。
- 本轮未写业务工作簿，未提交或推送；仓库原有未跟踪文件保持不变。

### 2026-08-12｜按新流转规则复核 2026-08-11 日清

- 隔离工作区：`D:\Users\lishang\Desktop\codex8.10\核销工作区_20260811_新流转规则复核_20260812_152900`；复用当前版本的 8 月 11 日智云四件套与已核验累计状态，未重放更早日期。
- 12 笔到账、42 条订单行：盈亏可写 0、幂等跳过 38、冲突 0、挂账 3、异常 1；写后再次校验仍为可写 0、跳过 38、冲突 0。
- 流转强三键自动项 8 笔、手填 4 笔；前置阶段实际补写 7 笔 SO+交付金额，1 笔已一致跳过。最终状态 6 笔“是”、2 笔空白、0 笔“部分”，因此本批没有红字 SO。
- 运行中发现 1 个历史已结账 SO 缺少智云交付金额传递；已增加安全闸，缺交付金额的整条到账改为手填，禁止只写 SO 不写金额。针对性回归 `16 passed`，源码及两个安装版该文件 SHA-256 一致。
- 最终 Excel 严格轻量审计通过；长期盈亏工作副本逐项放行基线既有 `W_EXTERNAL_LINKS`、`W_PIVOT_PARTS`，便携版和流转表只放行既有 `W_PIVOT_PARTS`，报表无警告。未提交或推送。

### 2026-08-12｜同一 SO 全部 SOD 结清后才计提

- 新规则：同一 SO 含多个 SOD 时，单个 SOD 结清不再立即计提；只有完整 SOD 集合全部结清且各 SOD 最新本币交付额齐全，才释放计提。
- 最后一个 SOD 结清时，当前计划同步补填此前已结清但计提为空的 SOD；一个 SOD 拆成多条业务行时，只在最后一条已结清行计提一次。历史非空计提与智云交付额不一致时写前冲突，禁止覆盖。
- 实现覆盖分类二次闸、写前逐行复核、OOXML 补丁写入、写后逐格回读和订单差异对象；不增加到账记录，不改变到账流转 AR 数量。
- 源码完整回归 `369 passed, 5 skipped`，仅有 2 条既有 Windows/GBK 子进程警告；`quick_validate.py` 通过。源码与 `D:\codex\skills\ar-hexiao-daily`、`C:\Users\lishang\.agents\skills\ar-hexiao-daily` 两处安装版关键文件 SHA-256 一致。
- 本轮未运行真实核销、未修改业务工作簿、未同步财务平台、未提交或推送；仓库原有未提交改动和未跟踪文件保持不变。

### 2026-08-12｜推送流转与 SO 计提最新改动

- 仅暂存并提交 `skills/ar-hexiao-daily` 的 15 个文件；仓库根 `PROJECT_CONTEXT.md` 和两个未跟踪金蝶 Skill 未进入提交，敏感信息扫描 0 命中。
- 提交 `c248b7546cebf57433fb56852c5bb00fd2bbd99c`（`feat(ar-hexiao): harden flow updates and SO accruals`）已推送到 Gitee 与 GitHub fork 分支 `agent/ar-hexiao-daily-1-3-0`，两个远端引用回读均与本地一致。
- GitHub 草稿 PR #1 已自动更新并包含该提交；推送完成时 `pytest` CI 正在运行。本轮未提交项目记录或其它 Skill。

### 2026-08-13｜发布 Skill 到本机 SkillHub

- 将 `skills` 下 16 个 Skill 发布到本机 SkillHub 命名空间 `besteasy-finance`，可见性均为 `NAMESPACE_ONLY`；逐项回读均存在，发布响应状态均为 `PUBLISHED`。
- 按用户中途指示停止后，`withholding-report-rename` 与 `xlsx` 未上传。
- 上传包排除了运行工作区、缓存、输出、浏览器配置、环境与会话文件，以及 `order-daily-summary/config/销售组织架构.xlsx`；源 Skill 文件未修改。
- `jdy-cashflow-export` 的两项敏感词提示经源码核对均为网页密码输入框对象变量，不含凭据；临时打包目录已移入回收站。
