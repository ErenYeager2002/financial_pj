# PR-00 证据报告（进行中）

## 范围

改动范围：scripts/refactor、docs/refactor、.github/workflows，以及 backend/tests/test_file_retention_guard.py 和新增 Native/写后失败测试；当前未修改业务运行路径。源码位于远程持久化 refactor-worktrees/full-platform-20260918，分支 refactor/full-platform-20260918。工作区不是当前生产源码；没有部署。

## 已有证据

- source-inventory.json/md：跟踪文件的静态结构；不能证明运行时可达性。
- typescript-inventory.json：TypeScript 编译器 AST 记录；不能代替类型检查。
- duplicate-candidates.json：精确重复与 Python AST 等价候选；未授权自动删除。
- 隔离与盘点已有 11 个合成测试通过，包括 tracked-only、敏感值不输出、symlink 排除和不执行被盘点源码。
- 后端实际基线在测试收集时失败，详见 ../baseline-failures.md。

## 未完成门禁

完整调用链与事务图、部署入口四层证据、合成样本、真实 PostgreSQL 基线、前端基线、CI 和双轴审查。PR-00 尚未通过，PR-01 尚不可启动。

## 回滚

本阶段无生产行为更改；生产无需回滚。保留审查报告，撤销工具代码不触碰业务材料或任务。

静态报告已补充 174 个装饰器路由、127113 个调用表达式、762 个事务候选位置、7998 条 import 记录；尚需语义分类，数量不代表实际可达路径。

近似重复检测新增同文件名 Python token 5-gram Jaccard 候选 301 对（阈值 0.8），不证明语义等价，不覆盖重命名文件。工具测试 14 项通过。部署入口四列证据见 ../deployment-entrypoints.md，计划任务及源码哈希仍待核验。

Linux 环境专项通过：fcntl 跨进程互斥及释放、权限 0600 的 Unix socket 跨进程请求响应。仅证明 OS 能力，不证明应用租约/鉴权正确。隔离及静态工具合成测试累计 18 项通过。迁移台账已列出 30 个文件，未连接生产数据库、未运行迁移。要求索引覆盖 22 PR / 90 T / 30 I / 16 G，但所有业务门禁仍须独立证据。

新增 CI 草案及测试 Dockerfile，已解析工作流结构，尚未在 GitHub 执行。测试矩阵保留 T01–T90，回滚矩阵提取规格原文且标记未演练；删除台账无业务删除。

冻结依赖前端复验已完成：类型、生产构建、生成契约及 3 组专项通过，格式/lint/4 个既有测试失败保持可见。新增合成材料生成器由当前解析器回读通过，工具测试累计 19 项。PR-00 尚缺 Native/Pi 成果 E2E、写后失败样本、部署入口完整核实及双轴审查。

Native UDS/产物服务回归已加入并执行，因新库 assistant_turns 缺表失败，未到达文件断言；保留为 PR-01 的可复现迁移缺口，未用测试建表绕过。

新增 test_refactor_postwrite_boundary.py 通过：在真实旧版暂存编排中注入写后 verify_sources 失败，确认暂存工作簿确实先被修改、归档未调用、年度与流转原件字节不变、暂存被清理。该证据不等于新版 14 阶段发布或 DB 事务验收。阶段双轴审查结果见 PR-00-review.md。
