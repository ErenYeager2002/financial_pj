# 项目开发约束

## 远程 Ubuntu 为修改入口

本项目后续所有代码、配置、业务 Skill 和项目文档改动，直接在远程 Ubuntu `192.168.30.46` 的持久化源码目录进行。当前部署根目录为 `/home/lee/financial-platform-isolated`；操作前核实当前发布与源码位置、已有差异和活动任务。不要在本地工作树实施后仅交付未同步补丁，也不要只修改会随容器重建丢失的容器文件。

本地目录仅维护连接入口、本项目约束及必要的只读参考；本条约束在本地和远程项目入口保持一致。远程无法连接时明确报告阻塞，不自行退回本地实施。用户授权的修改在远程完成，并明确报告源码、验证和运行生效状态；提交、推送及真实财务操作仍按当前任务的实际授权范围执行；部署和必要服务更新按下述持续授权执行，保留财务写前校验、副本、回读和日期顺序规则。

## 上下文与任务记录

本仓库采用单一领域上下文。

- 探索代码前读取 `CONTEXT.md`、`PROJECT_CONTEXT.md` 中与任务相关的当前记录，以及 `docs/adr/` 中的相关决策。历史记录不作为当前部署状态或持续授权的依据。领域文档规则见 `docs/agents/domain.md`。
- Issue 和规格以 Markdown 保存在 `.scratch/`，使用方式见 `docs/agents/issue-tracker.md`。
- Triage 标签保持为 `needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`，含义见 `docs/agents/triage-labels.md`。
- 本机 Codex 的 Skill 入口和允许名单由 `.codex/config.toml` 管理，维护说明见 `.codex/README.md`；它不控制平台业务 Skill 的发布或启停。

## 授权与自主执行

在已授权的对象、范围和条件内，主动完成必要的检查、准备、修改和适用验证；已有明确答案或授权不重复询问。可从当前文件和工具查明的信息先自行核实，只有缺失信息影响正确性、目标对象或授权范围时才澄清，并继续不依赖答案的工作。

保留适用流程中的明确审批点，审批前完成允许完成的准备并提供具体可审阅的结果。选择 Skill 或整理配置不构成提交、推送、真实核销或财务写入的授权；已有明确授权只对原定对象、范围和条件有效。财务写入仍须遵守对应业务流程的写前校验、副本、回读及日期先后规则，不自动重试失败的财务写入。

## 隐私与文件维护

临时脚本、测试导出、诊断日志、对比报告和临时备份统一保存在本地项目目录。必须在 Ubuntu 生成的临时文件，及时取回本地，核对文件清单和 SHA-256 后删除远程副本。远程仅保留必要的持久化源码、部署及回滚文件和正式业务运行数据；清理不得删除这些保留项。代码和配置仍直接在远程修改。

保护项目数据、个人信息和凭据，输出仅包含任务必要的信息。修改前检查当前文件及已有差异，保留与本任务无关的修改。财务原件、工作副本和交付文件按对应业务规则处理，不因代码维护扩大财务文件写入范围。

维护同名本地 Skill 时，遵守既有源目录与同步副本约定：先比较本次目标文件，保留并合并独立修改，只同步授权文件并核对哈希。平台内置副本、任务固定快照和远程发布各自处理，本地同步不自动发布。

## 修改必须在线上生效

用户对本项目后续代码、配置和业务 Skill 修改的授权，默认包含部署到当前远程 Ubuntu 线上环境、完成必要构建及更新相关服务，并核对实际生效状态。除非用户明确要求仅分析、仅源码或暂不部署，修改任务必须完成线上部署后才能交付，不以源码已改、补丁已保存或待部署作为完成。无需每次重复询问部署或必要服务更新。

部署前核实当前运行版本、目标文件差异及活动任务，保留可用回滚配置；活动任务影响安全切换时等待其完成，不中断或自动重跑财务任务。部署后回读相关 API、Worker、前端的版本或文件哈希，并完成与改动相关的健康及功能核验。部署失败时恢复可用版本，明确说明失败原因及未生效范围，不宣称完成。纯项目文档修改同步到对应持久化入口即可，不做无关服务重启。

本条是部署及必要构建、验证的持续授权，不扩大为提交、推送、真实核销重跑或财务数据写入授权；财务安全校验、副本、回读及日期顺序约束继续执行。

## 验证与交付

默认完成静态 diff 审查；为本次上线所必需的构建、编译及针对性验证属于上述持续授权。全量测试或无关验证不默认执行。用户明确限定只审查 diff 或不运行测试时按其限定执行，并说明实际验证及上线状态。

ask-matt 下游 Skill 中的 TDD、测试、提交和发布步骤同样服从本项目验证约定及用户实际授权。分别报告源码修改、静态审查、运行验证和部署状态，不将未执行的检查或未部署的改动宣称为已通过或已生效。有阻塞时说明已完成部分、未完成部分和原因。
