# 03 应收核销平台级单任务互斥

状态：已完成

## 交付

- 单日和批次创建使用同一数据库锁检查进行中任务。
- 等待人工取数检查、等待写入确认等暂停状态仍占用任务名额。
- 成功、失败或取消后释放名额。
- 多日期批次内部仍按既有顺序执行，不把子任务误判为外部并发任务。

## 验收证据

- 单日阻止单日、单日阻止批次、批次阻止单日的 API 测试通过。
- 终态任务不阻止新任务。

结果：单日、批次、旧对话入口、失败任务重置和失败批次重试均受同一锁保护；等待人工确认继续占用任务名额。
