# 09 固定展示手动补查并移除任务筛选

Status: ready-for-human
Category: enhancement
Priority: P2
Blocked by:

## Agent Brief

“我的任务”中的手动补查日期固定展示，不再使用展开收起控件。删除其下方的正式任务筛选表单和前端筛选查询状态，正式任务保留分页并始终显示全部任务。

## Acceptance criteria

- [x] 手动补查日期输入和操作固定可见，不再包含 `details` 或 `summary`。
- [x] 正式任务区域不显示状态、类型、Skill、业务日期或更新时间筛选。
- [x] 旧筛选查询参数不会继续影响“我的任务”结果，翻页链接只保留页码。
- [x] 后端任务中心查询接口和任务生命周期不变。
- [x] 任务提醒、任务中心、类型检查、代码规范和生产构建验证通过。

## Comments

2026-08-31：手动补查日期已改为固定展示区域，正式任务筛选表单及前端筛选查询状态已删除。旧筛选参数会重定向到仅保留页码的任务地址；后端接口未改。相关回归测试 28 项、类型检查、定向 oxlint 和 Next.js 生产构建通过；构建保留原有字体回退和 metadataBase 警告。
