# 银行流水自动对账

## 这项 Skill 做什么

读取两份 `.xlsx` 副本，根据金额容差和日期容差逐笔匹配，生成一份包含匹配、银行未匹配、总账未匹配三个工作表的结果文件。

## 人和平台怎么配合

员工上传银行流水和总账，填写或用自然语言说明容差。平台负责识别参数、校验文件和调度脚本；脚本负责确定性计算；员工下载结果后复核未匹配项目。

## 输入边界

当前版本不处理合并收款、拆分凭证和外币折算。这些情况会保留在未匹配清单中，等待人工复核。

